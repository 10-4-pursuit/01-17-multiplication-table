const number = 5; // Change this number to test with different multiplication tables

// TODO: Use a for loop to create and log the multiplication table for the given number

for (let i = 1; i <=10; i ++) {
    let result = number * i;
    console.log(`${number} * ${i} = ${result}`);
}